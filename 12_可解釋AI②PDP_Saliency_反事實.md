# 第 12 單元｜可解釋 AI（XAI）②：PDP／Saliency／反事實

> 單元目標：補齊剩餘 XAI 工具並能分辨 LIME/SHAP/PDP/Saliency/反事實。
> 對應考點：PDP（部分依賴圖）、Saliency Map（顯著性圖）、反事實解釋；XAI 四選一分辨。
> 時數：2 小時

---

## 12.1 回顧：XAI 工具箱

| 工具 | 作用 | 關鍵特性 |
|------|------|----------|
| LIME（第11單元） | 局部解釋 | 替代模型、Model-agnostic |
| SHAP（第11單元） | 特徵貢獻 | Shapley 值 |
| PDP | 特徵整體影響曲線 | **單一特徵、孤總體影響** |
| Saliency Map | 影像區域重要度 | 標示影響最大區 |
| 反事實（Counterfactual） | 輸入改變→結果改變 | 若怎樣改結果就變 |

本單元專講 PDP、Saliency、反事實。

---

## 12.2 PDP（Partial Dependence Plot，部分依賴圖）★常考

- **全名**：Partial Dependence Plots
- **作用**：畫出「某一特徵數值變化時，模型預測的平均變化」的曲線——看某個特徵對預測的**整體/平均影響**。
- 例：分析「溫度特徵」對產量預測的整體影響。
- **重要限制（高頻考點）**：**PDP 只能看「單一特徵」的影響，無法同時考慮「多個特徵」的交互作用**。

**考古題例**：
- 「製造業品質預測用 PDP 分析『溫度特徵』對品質的影響，PDP 的限制？」→ **只能分析單一特徵，無法同時考慮多特徵交互**。
- 「PDP 提供何種資訊？」→ 某一特徵對預測的**整體影響**。

> ⚠️ 最常考的 PDP「限制」：**單一特徵、無法處理多特徵並存交互**。

---

## 12.3 Saliency Map（顯著性圖）→ 影像 ★常考

- **全名**：Saliency Map（顯著性圖）
- **作用**：計算輸入中各「位置/像素」對模型輸出影響的大小，用**熱區標示影響最大的區域**。
- **適用**：**影像/權重類**模型——標示「影響判斷的區域」。
- 例：肺炎 X 光判讀時，標出肺部的關鍵異常區域。

**考古題例**：
- 「醫療 AI 用深度學習分析 X 光判斷肺炎，想要『明確標示影響判讀最大的區域』」→ **顯著性圖（Saliency Map）**。
- 「哪些工具主要用於影像、標示影響最大的區域」→ Saliency Map。

> 口訣：**「要標出影像上影響最大的區 → Saliency Map。」**

---

## 12.4 反事實解釋（Counterfactual）→ 改變輸入 ★常考

- **概念**：「反事實」＝「如果情況不同，結果會怎樣」。
- **作用**：回答「**改變哪個輸入，就會改變預測結果**」。
- 例：信貸被拒時，「若將收入提高某額度，就會通過」→ 給出可操作的改善方向。

**考古題例**：
- 「金融信貸決策導入反事實解釋的意義？」→ 說明**若改變哪些輸入（特徵）結果就會改變**，供決策回饋。
- 「反事實解釋最適合用來？」→ 高風險/決策需透明或改善建議（如信貸、醫療）。

> 口訣：**「若 A 改成 B → 結果變 = 反事實。」**

---

## 12.5 XAI 四選一分辨（核心技能）★

| 題目訊號 | 答案 |
|----------|------|
| 用替代模型、局部、與模型無關 | **LIME** |
| 各特徵的貢獻分數（Shapley 值） | **SHAP** |
| 單一特徵的整體影響曲線 | **PDP** |
| 影像上標示影響最大的區域 | **Saliency Map** |
| 若改變哪些輸入結果就改變 | **反事實（Counterfactual）** |

**解題要領**：
- 出現「**替代模型/局部/Model-agnostic**」→ LIME。
- 出現「**貢獻度分數/Shapley**」→ SHAP。
- 出現「**單一特徵/無法多特徵**」→ PDP。
- 出現「**影像/區域/熱區**」→ Saliency。
- 出現「**如果輸入改變→結果改變**」→ 反事實。

---

## 12.6 考古題實戰

**1.** 用 PDP 看出「溫度特徵」對品質預測的影響，PDP 的限制？
(A) 無法顯示任何影響  (B) **只能單一特徵、無法同時考慮多特徵交互** ✓  (C) 只能影像  (D) 只能分類
→ 選 B。

**2.** 醫療 AI 分析 X 光，要標示影響判讀最大的區域？
(A) LIME  (B) SHAP  (C) **Saliency Map** ✓  (D) PDP
→ 選 C。

**3.** 信貸被拒，想知道「若改變哪些輸入結果就會通過」？
(A) PDP  (B) **反事實（Counterfactual）** ✓  (C) LIME  (D) Saliency
→ 選 B。

**4.** 要得到「每一輸入特徵對單筆預測的貢獻分數」？
(A) PDP  (B) **SHAP** ✓  (C) Saliency  (D) 反事實
→ 選 B。

**5.** 下列哪個工具最主要用於「影像」的解釋？
(A) PDP  (B) SHAP  (C) LIME  (D) **Saliency Map** ✓
→ 選 D。

---

## 12.7 活動與練習（2 小時配置建議）

- **00:00–00:20** 講授 12.1–12.2（回顧、PDP）
- **00:20–00:40** 講授 12.3–12.4（Saliency、反事實）
- **00:40–01:00** 講授 12.5（四選一分辨表）＋案例討論
- **01:00–01:30** XAI 綜合分辨測驗（10 題，混合 LIME/SHAP/PDP/Saliency/反事實）
- **01:30–01:52** 檢討、易錯
- **01:52–02:00** 預告第 13 單元（LLM① RAG）＋收尾

---

## 12.8 本單元重點速記

- **PDP**＝單一特徵整體影響曲線；**限制：無法多特徵交互**。
- **Saliency Map**＝影像上標示影響最大區域。
- **反事實**＝若改變哪些輸入結果就改變（提供改善方向）。
- 分辨口訣：**替代模型→LIME；貢獻分數→SHAP；單特徵→PDP；影像區域→Saliency；改變輸入→反事實。**

> 下一單元預告：第 13 單元「大型語言模型 LLM①」——RAG 與 Fine-tuning。
