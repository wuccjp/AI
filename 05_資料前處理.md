# 第 5 單元｜資料前處理：正規化、編碼、遺漏值與特徵工程

> 單元目標：掌握「哪些演算法需要哪種前處理」，能判斷正規化、編碼、遺漏值處理的正確做法。
> 對應考點：標準化/正規化、One-Hot/Label/Target/Binary 編碼、遺漏值插補、正則化。
> 時數：2 小時

---

## 5.1 為什麼要前處理

- 原始資料常含缺失、尺度不一、類別文字，直接丟給模型會出問題。
- 前處理目的：讓資料「乾淨、尺度一致、可被模型使用」。

---

## 5.2 標準化 vs 正規化 ★核心考點

**目標**：讓數值特徵有相似的尺度。

| 方法 | 作法 | 適合演算法 |
|------|------|-----------|
| **標準化（Standardization）** | 減平均／除標準差 → 平均值0、標準差1 | 依賴「距離/尺度」的演算法：**SVM、KNN、K-Means** |
| **正規化（Normalization）** | 縮放到 [0,1] 區間 | 同理，距離型演算法較有感 |

### 重點：哪些演算法對尺度敏感？
- **敏感**（需標準化）：**SVM、KNN、K-Means**——因為它們算距離，尺度大的特徵會主導。
- **較不敏感**：**決策樹（Decision Tree）、隨機森林（Random Forest）**——以切分為主，不受尺度影響。

**考古題**：
- 「哪種模型最適合在不做標準化的情境下使用數值特徵？」→ 決策樹/Random Forest。
- 「標準化最適合配哪種演算法？」→ SVM/KNN/K-Means。

> 口訣：**「距離看尺度、樹不理尺度」**。

---

## 5.3 類別特徵編碼（Encoding）★常考

類別文字（星期幾、職業、地區）需轉成數字。

| 方法 | 作法 | 適用/注意 |
|------|------|-----------|
| **One-Hot Encoding** | 每個類別展開為 0/1 欄位 | 無序類別；但**高基數(稀疏)類別會欄位爆炸** |
| **Label Encoding** | 依序號整數標記（0,1,2...） | 適合**有序類別**；無序類別亂給整數會誤導模型 |
| **Binary Encoding** | 轉成二元碼 | 高基數、壓縮欄位數 |
| **Target Encoding** | 以目標值的平均取代類別 | 壓縮欄位，但**易造成資料洩漏/過擬合** |

### 高頻考點
- **稀疏/高基數類別（大量類別）** → 別用 One-Hot（欄位爆炸），改用 **Target/Binary**。
- **Target Encoding 風險**：用「目標值」編碼可能**洩漏答案→過擬合**（考古題考過「哪種編碼最可能導致讀不到……或過擬合」）。
- **决策樹/隨機森林對編碼相對不敏感**：One-Hot vs Label 對樹影響較小。

**考古題例**：
- 「資料含大量稀疏類別特徵，最適合哪種編碼？」→ 避免 One-Hot，選 Target/Binary。
- 「哪種編碼最可能因使用目標值而導致過擬合（資料洩漏）？」→ **Target Encoding**。
- 距今有「職業類別」等無序高基數特徵→ 用 Binary/Target，不用 One-Hot。

---

## 5.4 遺漏值（Missing Value）處理 ★常考

| 做法 | 說明 | 何時用 |
|------|------|--------|
| 移除（刪除）該列/欄 | 直接刪 | 缺很少、且刪掉不影響代表性 |
| 填 0 | 補零 | 常用但不佳（會扭曲分布） |
| **插補（Imputation，前後值/內插）** | 用鄰近值補 | **時間序列**尤佳 |
| 填平均/中位數 | 用統計值補 | 一般表格 |

### 時間序列遺漏重點
- 考古題：**感測器溫度時序資料，5% 出現遺漏，確認是短暫失聯（非系統性）**。
- 正確做法：**以前一/後一時點的值做插補（線性/前值插補）**，而非補 0 或整段刪除。
- 「補 0」是錯誤選項（會扭曲時間趨勢）。

---

## 5.5 正則化（Regularization）：L1/L2 ★進階常考

- **正則化目的**：抑制過擬合、避免參數過大。
- **L1（Lasso）**：會把不重要的參數**收斂為 0** → 天然做「特徵選擇」。
- **L2（Ridge）**：把參數縮小但不歸零。

**考古題**：
- 「Lasso 模型中，L1 正則化導致參數收斂為零的意義？」→ 目的是**特徵選擇／消除無關特徵**。

> 口訣：**L1=稀疏、砍特徵；L2=縮小、不歸零。**

---

## 5.6 資料失衡（Imbalanced Data）與混淆

- **類別不平衡**：如詐欺樣本比例極低。
  - 問題：模型學成「全猜多數類」，準確率高但抓不到少數類。
  - 處理：欠抽樣/過抽樣、調整權重、改用 Recall/F1（而非只看 Accuracy）。
- 呼應第 10 週「評估指標」。

**考古題例**：
- 「詐欺樣本比例極低導致模型……」→ 這與過擬合、判斷指標有關（詳見第 10 週）。

---

## 5.7 考古題實戰

**1.** 下列哪一種模型最適合在不做標準化（Standardization）的情況下使用數值特徵？
(A) SVM  (B) KNN  (C) K-Means  (D) **決策樹** ✓
→ 選 D。樹模型對尺度不敏感。

**2.** 資料含大量稀疏類別特徵，最適合採用？
(A) One-Hot Encoding  (B) **Target/Binary Encoding** ✓  (C) Label 亂給  (D) 全刪除
→ 用 Target/Binary 壓縮欄位、避免 One-Hot 爆炸。

**3.** 哪種編碼最可能因「使用目標值」而導致過擬合？
(A) One-Hot  (B) **Target Encoding** ✓  (C) Label  (D) 不做編碼
→ Target Encoding 洩漏答案。

**4.** 感測器溫度時序資料 5% 遺漏（短暫失聯），最合適處理？
(A) 補 0  (B) **前一/後時點插補** ✓  (C) 全刪該段  (D) 補平均值
→ 時間序列用前後插補。

**5.** Lasso 的 L1 正則化使參數收斂為零，主要目的是？
(A) 增加參數  (B) **特徵選擇/消除無關特徵** ✓  (C) 提升精度  (D) 加速
→ 選 B。

---

## 5.8 活動與練習（2 小時配置建議）

- **00:00–00:20** 講授 5.1–5.2（前處理、標準化/正規化+適用演算法）
- **00:20–00:40** 講授 5.3（類別編碼）
- **00:40–00:55** 講授 5.4–5.5（遺漏值、正則化）
- **00:55–01:15** 實作：小資料集做標準化、One-Hot、前後插補，觀察結果
- **01:15–01:40** 考古題測驗 10 題
- **01:40–01:52** 檢討、易錯題講解
- **01:52–02:00** 預告第 6 週（監督式演算法）＋收尾

---

## 5.9 本週重點速記

- 標準化適合 **SVM/KNN/K-Means**；**決策樹/隨機森林不敏感**。
- **高基數稀疏類別**：別用 One-Hot，用 Target/Binary。
- **Target Encoding 洩漏→過擬合**。
- **時間序列遺漏**：用前後值**插補**，別補 0。
- **L1/ Lasso**＝特徵選擇（參數歸零）；**L2**＝縮小。
- 類別不平衡 → 慎用 Accuracy，重視 Recall/F1（接第 10 週）。

> 下一週預告：第 6 週「監督式演算法」——迴歸、分類、決策樹、KNN、貝氏分類器。
