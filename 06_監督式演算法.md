# 第 6 單元｜監督式演算法

> 單元目標：理解常用監督式模型（迴歸、分類、決策樹、KNN、貝氏）的特性與適用情境，
> 並能判斷損失函數、過擬合與演算法細節。
> 對應考點：監督式演算法特性、損失函數、KNN、貝氏分類器、偏差與變異權衡。
> 時數：2 小時

---

## 6.1 監督式回顧與任務

- 監督式：有標籤，學「輸入→答案」。
- 兩大任務：
  - **迴歸（Regression）**：輸出連續數值。
  - **分類（Classification）**：輸出類別。
- 本單元講幾個「初級科會考」的監督式演算法。

---

## 6.2 線性迴歸（Linear Regression）與損失函數 ★常考

- 找一條直線（或超平面）擬合輸入與輸出的關係。
- 常用於：房價、理賠金額、需求、住院日數等**連續值**預測。
- **損失函數（Loss Function）**：衡量模型預測誤差，訓練時最小化它。
  - 迴歸常用 **MSE（均方誤差）**。
  - 有極端值/偏態時，**MAE（平均絕對誤差）或中位數迴歸**較穩健。

**考古題例**：
- 「預測房價應選用哪種損失函數？」→ 迴歸損失（**MSE** 等），而非分類損失（如 Cross-entropy）。
- 「多數病人住院日數偏態（少數特長）→ 預測住院日數」→ 用**中位數**或 MAE 較穩健（不要被極端值影響）。

> 口訣：**迴歸→MSE/MAE；分類→交叉熵（本單元記迴歸即可）**。

---

## 6.3 分類演算法概覽

| 演算法 | 特性 | 適用 |
|--------|------|------|
| 羅吉斯迴歸 | 線性分類器 | 二元分類（是否違約/購買） |
| 決策樹（Decision Tree） | 以 if-else 切分、**對尺度不敏感** | 可解釋、混合型資料 |
| 隨機森林（Random Forest） | 多棵決策樹投票、更穩健 | 泛化較好 |
| KNN | 依最近鄰投票、**需距離、對尺度敏感** | 較小資料、需標準化 |
| 貝氏分類器（Naive Bayes） | 機率式、假設特徵獨立 | **文字分類、垃圾郵件** |

---

## 6.4 KNN（K-近鄰）★可考計算

- **作法**：新樣本看它「特徵空間裡最近的 K 個鄰居」，由鄰居多數類別決定分類。
- **關鍵**：需要距離計算 → **對特徵尺度敏感，要先標準化**。
- **K 的影響**：
  - K 太小（如 1）→ 易受雜訊影響、易**過擬合**。
  - K 太大 → 過度平滑、易欠擬合。
- **KNN 計算題**：給定鄰居座標/類別與 K，判斷分類。

**考古題例**：
- 「KNN 設 K=3，新的測試點最近的 3 個鄰居是 [A,A,B]，分類為何？」→ A（多數決）。

---

## 6.5 貝氏分類器（Naive Bayes）★常考

- **Naive Bayes**：基於貝氏定理，並「假設特徵彼此獨立（Naive）」，用條件機率分類。
- **典型應用**：**文字分類、垃圾郵件過濾、情緒分析**。
- 為何常用在 NLP：文字特徵（詞出現與否）彼此「簡化假設」下仍有效。

**考古題例**：
- 「貝氏分類器常被應用於何處？」→ **文字分類/垃圾郵件/NLP**。

---

## 6.6 偏差與變異權衡（Bias-Variance Tradeoff）★常考

- **偏差（Bias）**：模型太簡單，學不到規律 → **欠擬合（Underfitting）**。
- **變異（Variance）**：模型太複雜，過度敏感於訓練資料 → **過擬合（Overfitting）**。
- **權衡**：要在兩者間取平衡，降低總誤差。

**判讀**：
- 訓練與測試都很差 → 高偏差（欠擬合）→ 增加複雜度。
- 訓練很好、測試差 → 高變異（過擬合）→ 降低複雜度/正則化/交叉驗證。

**考古題**：
- 「偏差與變異權衡主要用於說明？」→ 模型複雜度與誤差的平衡、欠擬合 vs 過擬合。
- 「訓練準確率 98%、測試低」→ 過擬合（詳見第 10 單元）。

---

## 6.7 監督式模型選擇速查

| 情境 | 建議模型 |
|------|----------|
| 連續數值預測（房價/需求） | 線性迴歸（MSE） |
| 二元分類（是否購買/違約） | 羅吉斯迴歸/決策樹 |
| 需可解釋 | 決策樹 |
| 文字/垃圾郵件 | 貝氏分類器 |
| 小資料、無需訓練的即時判別 | KNN（需標準化） |
| 要穩健泛化 | 隨機森林 |

---

## 6.8 考古題實戰

**1.** 預測房價，應選用哪種損失函數？
(A) Cross-entropy  (B) **MSE** ✓  (C) 想想  (D) 準確率
→ 選 B。迴歸用 MSE。

**2.** 依病患年齡、血壓、BMI 判斷是否罹病：
(A) 迴歸  (B) **分類** ✓  (C) 分群  (D) 異常偵測
→ 輸出「是/否」類別 = 分類。

**3.** 貝氏分類器最常用於？
(A) 影像  (B) **垃圾郵件/文字分類** ✓  (C) 語音  (D) 排程
→ 選 B。

**4.** KNN 設 K=3，最近 3 個鄰居為 [A,A,B]，測試點分類？
(A) A ✓  (B) B  (C) 無法判斷  (D) 亂數
→ 選 A。多數決。

**5.** 訓練準確率高、測試低，屬於？
(A) 欠擬合  (B) **過擬合** ✓  (C) 正常  (D) 偏差高
→ 選 B。高變異過擬合。

---

## 6.9 活動與練習（2 小時配置建議）

- **00:00–00:20** 講授 6.1–6.2（任務、線性迴歸+損失函數）
- **00:20–00:40** 講授 6.3–6.4（分類演算法、KNN 與計算）
- **00:40–00:55** 講授 6.5–6.6（貝氏、偏差變異權衡）
- **00:55–01:15** 實作：小資料集跑 KNN/決策樹/隨機森林，觀察過擬合與尺度影響
- **01:15–01:40** 考古題測驗 10 題（含 KNN 計算）
- **01:40–01:52** 檢討
- **01:52–02:00** 預告第 7 單元（非監督/強化）＋收尾

---

## 6.10 本單元重點速記

- 迴歸→連續值、損失 **MSE**；分類→類別。
- **KNN**＝多數決、需**標準化**、K 小易過擬合。
- **貝氏**→文字/垃圾郵件/NLP。
- **決策樹/隨機森林**對尺度不敏感、較可解釋。
- 偏差高=欠擬合；變異高=過擬合（訓練好測試差）。

> 下一單元預告：第 7 單元「非監督式與強化學習」——分群、異常偵測、Q-Learning/Deep Q-Learning。
