# 第 14 單元｜大型語言模型 LLM②：推論優化與新技術

> 單元目標：認識 115 年次新增的 LLM 技術名詞（推論架構、效能優化、自監督、強化式優化）。
> 對應考點：批次/即時推論、Flash Attention、稀疏 MoE、Pruning、120B 參數、CLIP/DINO、GRPO、AutoSkill。
> 時數：2 小時

---

## 14.1 LLM 推論架構：批次 vs 即時 ★常考

- **推論（Inference）**：訓練後用模型做預測/生成答案。
- **批次推論（Batch Inference）**：把大量請求**分批**收集後一次處理。
  - 適合：**非即時**、大量、可等待的任務（如每日報表、批量資料處理）。
  - 優點：吞吐量高、成本低。
- **即時推論（Real-time Inference）**：**即時、低延遲**回應單一請求。
  - 適合：線上客服、即時搜尋、需立即回應。
  - 優點：低延遲；缺點：需更多資源/成本高。

**考古題例**：
- 「批次推論與即時推論的差異」→ 批次：大量分批、可等待、成本低；即時：低延遲、需資源。
- 「某雲端公司把 LLM 以批次推論部署於線上系統」→ 探討吞吐/成本/延遲取捨。

---

## 14.2 Transformer 長期輸入的推理瓶頸 ★常考

- Transformer 的**注意力（Attention）**在長輸入時運算量約「平方成長」。
- **非常長的輸入**推理會**慢、耗記憶體、成本高**。
- 這是優化技術（Flash Attention 等）出現的原因。

**考古題例**：
- 「對非常長輸入做推理，Transformer 的問題？」→ **注意力運算成本高、推理慢**。

---

## 14.3 Flash Attention（注意力優化）★常考

- **Flash Attention**：一種**優化注意力計算**的技術，減少記憶體讀寫、加快運算。
- 用於**改善 Transformer 推論/訓練效率**。
- 不改變架構本質，只優化計算。

**考古題例**：
- 「LLM 效能優化中，Flash Attention 常被用來？」→ **改善/優化 Transformer 的注意力計算效率**。

---

## 14.4 稀疏混合專家（Sparse Mixture-of-Experts, MoE）★常考

- **MoE**：模型中包含多個「專家（Expert）」子網路，**每個輸入只啟動部分專家**（稀疏）。
- **優勢**：**不增加每步推理的總計算量**下擴大模型參數量——提升效率與容量。
- 對比：全參數模型每個輸入都動用所有參數；MoE 只動部分。

**考古題例**：
- 「採用 Sparse MoE 架構，每一輸入只使用部分的專家、以提升效率」→ **MoE**。
- MoE「每步只啟動部分參數」以**提升推理效率**——這是核心考點。

> 口訣：**MoE = 專家分工，每輸入只動一部分。**

---

## 14.5 剪枝（Pruning）與壓縮

- **剪枝（Pruning）**：移除模型中不重要的參數/連接，**縮小模型**、減少推理成本。
- 對比 MoE（在不縮小下提升效率）與剪枝（把模型變小）。

**考古題**：
- 「大型 Transformer 效能優化中常見的『剪枝』？」→ 移除不必要參數以縮小模型、降低成本。

---

## 14.6 大型模型的部署考量：120B 參數 ★常考

- 超大參數模型（如 120B）帶來**部署挑戰**：
  - **算力/資源需求巨大**。
  - **成本高、推理慢**。
  - 資料隱私（資料送雲端 vs 本地部署）。
- 需在「效能與成本/算力/隱私」間取捨。

**考古題例**：
- 「採用如 120B 參數的大型 LLM 時，最需考量的因素？」→ **運算/記憶體資源與成本**（另有隱私）。
- 有時選「推理成本與算力資源」相關選項。

---

## 14.7 自監督學習（Self-supervised Learning）：CLIP／DINO ★新名詞

- **自監督學習**：不需人工標籤，從**資料本身構造監督訊號**來學習。
  - 例：CLIP 用「文字與影像」的配對關係自監督。
  - DINO 用「同一影像的擴增版本彼此一致」自監督。
- 對比**監督式**（需標籤）。

**考古題例**：
- 「CLIP 與 DINO 皆屬自監督學習（Self-supervised），其訓練資料有何不同？」
  → **CLIP 使用影像與文字配對資料**；**DINO 使用無標籤的影像資料**（自監督）。
  正確選項通常是「CLIP 用影像+文字對、DINO 用無標籤影像」。

---

## 14.8 強化式策略優化：GRPO ★新名詞

- **GRPO（Group Relative Policy Optimization）**：強化學習的**策略優化方法**。
- 用於**優化大型語言模型**在特定任務（如推理）上的策略能力。
- 屬**強化式**（靠回饋/獎勵優化策略）。

**考古題例**：
- 「GRPO 最適合用來強化模型在下列哪一方面？」→ 大型語言模型的**推理任務能力**。

---

## 14.9 AutoSkill 等框架（進階新知）

- 新框架/新方法在考題中偶爾出現（如 AutoSkill），主要考「**名稱對上用途**」。
- 作法：記住說明文字「把碎片化能力整合成可重用的技能/工具」，選字形相符的描述。
- 這類題較偏「新知」，若選項敘述模糊，優先選「把多個 AI/技能整合」或「強化式自動優化」類。

---

## 14.10 考古題實戰

**1.** Flash Attention 主要用途？
(A) 增加參數  (B) **優化 Transformer 注意力計算、提升效率** ✓  (C) 影像分類  (D) 生成無關
→ 選 B。

**2.** Sparse MoE 的特色？
(A) 每輸入動用所有參數  (B) **每輸入只啟動部分專家、提升效率** ✓  (C) 只能影像  (D) 不需訓練
→ 選 B。

**3.** 批次推論適合？
(A) 即時低延遲  (B) **大量非即時、可等待、成本低** ✓  (C) 單一即時  (D) 無法批次
→ 選 B。

**4.** 120B 大型 LLM 部署時最需考量？
(A) 成本高低無所謂  (B) **運算/記憶體資源與成本** ✓  (C) 不需考量隱私  (D) 一定免費
→ 選 B。

**5.** CLIP 與 DINO 同屬自監督，差異在？
(A) 兩者皆監督式  (B) **CLIP 用影像+文字對、DINO 用無標籤影像** ✓  (C) 兩者皆需標籤  (D) 差異在參數
→ 選 B。

**6.** GRPO 屬於？
(A) 監督式  (B) **強化式策略優化** ✓  (C) 非監督分群  (D) 無監督生成
→ 選 B。

---

## 14.11 活動與練習（2 小時配置建議）

- **00:00–00:20** 講授 14.1–14.2（批次/即時、長輸入瓶頸）
- **00:20–00:45** 講授 14.3–14.5（Flash Attention、MoE、剪枝）
- **00:45–01:05** 講授 14.6–14.7（120B、自監督 CLIP/DINO）
- **01:05–01:25** 講授 14.8–14.9（GRPO、AutoSkill新知）
- **01:25–01:50** 考古題測驗 10 題（新名詞）
- **01:50–02:00** 檢討、預告第 15 單元（法規治理）＋收尾

---

## 14.12 本單元重點速記

- **批次推論**＝大量非即時、成本低；**即時推論**＝低延遲、資源高。
- **Flash Attention**＝優化注意力計算效率。
- **Sparse MoE**＝每輸入只啟動部分專家、提升效率。
- **剪枝（Pruning）**＝移除參數縮小模型。
- **120B**＝考量算力/成本/隱私。
- **自監督**：CLIP＝影像+文字對；DINO＝無標籤影像。
- **GRPO**＝強化式優化 LLM 推理；AutoSkill 為新知框架。

> 下一單元預告：第 15 單元「AI 治理、法規與負責任 AI」——《基本法》、金融AI規範、評測中心、個資、倫理原則。
