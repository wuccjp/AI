# 第 8 單元｜深度學習架構①：CNN／RNN／LSTM

> 單元目標：辨識 CNN、RNN、LSTM 三種架構的適用情境與特性。
> 對應考點：影像→CNN、序列/語音→LSTM、RNN 長期依賴問題、深度學習概念。
> 時數：2 小時

---

## 8.1 深度學習簡介

- **深度學習（Deep Learning）**：多層神經網路，能**自動從大量資料中學習高階特徵**（不需人工設計特徵）。
- 與傳統 ML 差異：傳統需人工做特徵工程；深度學習自動學習特徵。
- 特性：
  - **通常需要大量資料**才能表現好。
  - **運算成本高**。
  - 透過「多個隱藏層」逐層抽取特徵。

**考古題例**：
- 「關於深度學習何者最正確？」→ 「透過多個隱藏層自動從大量資料學習高階特徵」為正確，其他（如「不需大量資料」「需人工設計特徵」）皆錯。

---

## 8.2 CNN（卷積神經網路）→ 影像 ★核心

- **CNN（Convolutional Neural Network）**：透過「卷積」自動提取影像的空間特徵（邊緣、紋理、形狀）。
- **適用**：任何與**影像/空間結構**有關的任務。
- **典型應用**：
  - PCB 電路板缺陷檢測
  - 布料/零件瑕疵檢測
  - 人臉辨識、肺炎 X 光判讀
  - 作物葉片病害、蘋果成熟度
- **關鍵字**：影像、圖像、缺陷、瑕疵、X 光、攝像頭監視畫面。

**考古題例**：
- 「電子製造用 AI 辨識 PCB 電路板製程缺陷」→ **電腦視覺/CNN**。
- 「紡織布料瑕疵檢測」→ **CNN**。
- 「智慧交通用監視攝影即時識別汽車」→ **CNN**。

> 口訣：**「跟圖像有關 → CNN。」**

---

## 8.3 RNN（循環神經網路）→ 序列

- **RNN（Recurrent Neural Network）**：專處理「序列/順序」資料，把前一步輸出帶入下一步。
- **適用**：時序資料、自然語言、語音。
- **問題（考核點）**：RNN 在處理**長序列**時有**長期依賴**問題——距離太遠的資訊無法保留（梯度消失/爆炸）。
- **應用**：語音、文本、時間序列。

**考古題**：
- RNN 在建模時可能難以保留「長時間序列前面的重要資訊」→ 這是 RNN 的長期依賴缺點。

---

## 8.4 LSTM（長短期記憶）→ 長序列/語音 ★核心

- **LSTM（Long Short-Term Memory）**：改進 RNN 的架構，特別設計「記憶單元」解決長期依賴問題。
- **適用**：需要記住**長時間前資訊**的長序列：
  - 長語音序列
  - 長文本
  - 需保留長時間順序的任務
- **與 RNN 區別**：LSTM **改善 RNN 無法保留長期記憶**的問題。

**考古題例**（重複出現）：
- 「語音辨識需分析長時間語音序列，句子長時模型難保留前面資訊，應選？」→ **LSTM**。
- 「傳統 RNN 在建模時難以保留長時間序列前面的重要資訊，哪種架構可解決？」→ **LSTM**。

> 口訣：**「長序列/語音/要記很久→LSTM；一般序列→RNN；影像→CNN。」**

---

## 8.5 架構選擇速查表

| 資料 | 架構 |
|------|------|
| 影像/空間特徵 | **CNN** |
| 一般序列/時序 | RNN |
| 長序列/語音/長期記憶 | **LSTM** |
| 文字→語言理解/長文 | Transformer（第 9 單元） |

---

## 8.6 深度學習常見課題

- **梯度消失/爆炸（Vanishing/Exploding Gradient）**：層數深時，梯度變很小或爆炸，訓練困難。
  - LSTM 緩解 RNN 的梯度問題；Batch Normalization 也有助穩定（第 9 單元）。
- **過擬合**：資料量不足/模型太複雜 → 訓練好測試差（第 10 單元詳述）。
- **Batch Normalization**：正規化每一層輸入，加速收斂、較穩定、減緩梯度問題。

---

## 8.7 考古題實戰

**1.** 語音辨識處理長時間語音，句子長時難保留前面資訊，應選？
(A) CNN  (B) RNN  (C) **LSTM** ✓  (D) 貝氏
→ 選 C。

**2.** 電子製造辨識 PCB 電路板製程缺陷：
(A) **CNN** ✓  (B) LSTM  (C) RNN  (D) Autoencoder
→ 選 A。影像→CNN。

**3.** 傳統 RNN 建模難保留長時間序列前面的重要資訊，哪種架構可改善？
(A) CNN  (B) **LSTM** ✓  (C) FCNN  (D) 決策樹
→ 選 B。

**4.** 下列關於深度學習何者正確？
(A) 需大量人工設計特徵  (B) **多層神經網路自動學習高階特徵** ✓  (C) 不需大量資料  (D) 單層即可
→ 選 B。

**5.** 智慧交通用監視攝影機即時識別畫面中的汽車、行人：
(A) **CNN 電腦視覺** ✓  (B) LSTM  (C) NLP  (D) 異常偵測
→ 選 A。

---

## 8.8 活動與練習（2 小時配置建議）

- **00:00–00:20** 講授 8.1–8.2（深度學習概念、CNN）
- **00:20–00:40** 講授 8.3–8.4（RNN、LSTM 與差異）
- **00:40–00:55** 講授 8.5–8.6（選擇速查、梯度課題）
- **00:55–01:20** 實作/案例：觀察 CNN 對影像、LSTM 對語音的區分；討論「影像 vs 序列」判讀
- **01:20–01:45** 考古題測驗 10 題
- **01:45–01:55** 檢討、易錯
- **01:55–02:00** 預告第 9 單元（Transformer/GAN/Autoencoder）＋收尾

---

## 8.9 本單元重點速記

- **深度學習**＝多層神經網路自動學習高階特徵（需大量資料）。
- **CNN→影像**（缺陷/瑕疵/X光/監視）。
- **RNN→序列**（有時序/語言），但有**長期依賴問題**。
- **LSTM→長序列/語音**（改善 RNN 長期記憶）。
- **口訣**：影像→CNN；長序列/語音→LSTM；一般序列→RNN。

> 下一單元預告：第 9 單元「深度學習架構②」——Transformer（LLM 基礎）、GAN（生成）、Autoencoder/VAE（壓縮重建/異常）。
