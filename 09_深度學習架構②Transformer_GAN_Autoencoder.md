# 第 9 單元｜深度學習架構②：Transformer／GAN／Autoencoder

> 單元目標：掌握生成與注意力架構，辨識 Transformer、GAN、Autoencoder/VAE 的特性與應用。
> 對應考點：Transformer（LLM 基礎）、GAN（生成）、Autoencoder（異常/重建）、Batch Normalization、GRPO。
> 時數：2 小時

---

## 9.1 Transformer（注意力機制）→ LLM 基礎 ★核心

- **Transformer**：以「注意力機制（Attention）」為核心的架構，是 **LLM（如 GPT、Gemini）的基礎**。
- **特性**：
  - **可平行處理**整個輸入（不須像 RNN 逐字依序）。
  - 能捕捉**長距離的文字相依**。
  - 效率高於 RNN 的序列處理。
- **限制**：長輸入的**注意力運算成本高、推理慢**。

**考古題例**：
- 「ChatGPT、Gemini 對話型生成式 AI 所依據的基礎模型技術？」→ **Transformer/LLM**。
- 「LLM 由哪種架構為基礎？」→ Transformer（注意力機制）。

### Transformer 處理長文的特性考點
- 對「非常長輸入」做推理時，**注意力計算成本高、可能較慢**（考點）。
- Transformer **可平行處理（Parallel）** 而非僅依序——這是一大優勢。

---

## 9.2 GAN（生成對抗網路）→ 生成 ★核心

- **GAN（Generative Adversarial Network）**：由**生成器（Generator）與判別器（Discriminator）**互相對抗。
  - 生成器：企圖生成以假亂真的資料。
  - 判別器：判斷資料真假。
  - 兩者「對抗」使生成器越生越真。
- **應用**：
  - 影像**生成**、風格轉換
  - 照片轉公仔風格
  - **資料擴增**（生成更多樣本）
- **關鍵字**：生成、合成、風格轉換、假樣本、資料擴增。

**考古題例**：
- 「小明用 AI 將照片轉成公仔風格影像」→ **GAN**（生成/風格轉換）。
- 「哪種架構透過兩個模型互相對抗來生成影像」→ GAN。

> 口訣：**生成/風格轉換→GAN。**

---

## 9.3 Autoencoder（自編碼器）與 VAE → 壓縮重建/異常 ★進階

- **Autoencoder**：Encoder 把輸入壓縮成低維潛在特徵，Decoder 還原。
  - 用途：**降維、去雜訊、異常偵測（重建誤差）**。
- **VAE（變分自編碼器）**：Autoencoder 的**生成式變體**，在潛在空間加上機率限制，可**生成**新樣本。
- **異常偵測**：只學正常的重建，異常樣本重建誤差大 → 判異常（詳見第 7 週）。

**考古題例**：
- 「關於 VAE 運作流程」→ Encoder 壓縮→潛在分布→Decoder 重建的生成流程。
- 「Autoencoder 最適合的應用」→ 無監督異常偵測、降維。

> 口訣：**重建/壓縮/異常→Autoencoder；生成式變體→VAE。**

---

## 9.4 Batch Normalization → 訓練穩定 ★常考

- 對每一層的輸入做**標準化（正規化）**，讓各層輸入尺度一致。
- **作用**：加速收斂、穩定訓練、**減緩梯度消失/爆炸／減緩內部共變異(Covariate Shift)**。
- 讓深層網路更好訓練。

**考古題例**：
- 「在深度網路加入 Batch Normalization 最主要作用？」→ **將輸入正規化、使各層特徵尺度一致，穩定/加速訓練**、減緩梯度問題。

> ⚠️ 選項要選「正規化輸入、穩定訓練」，別選「增加非線性/加速推理」。

---

## 9.5 強化式策略優化：GRPO（進階，115 新名詞）

- **GRPO（Group Relative Policy Optimization）**：一種基於**強化學習**的模型訓練/優化方法。
- **用途**：用於提升**大型語言模型**在特定任務上的能力（如推理）。
- **屬強化式**（靠回饋/獎勵優化策略），非監督式/監督式。

**考古題例**：
- 「GRPO 最適合用於強化模型在哪一方面的表現？」→ 大型語言模型的**推理/任務能力**（透過強化式優化策略）。

---

## 9.6 架構總覽速記表

| 架構 | 特性 | 主要用途 | 關鍵關鍵字 |
|------|------|----------|-----------|
| Transformer | 注意力、平行、長距離 | LLM 基礎、語言理解 | 注意力、平行、LLM |
| GAN | 生成器+判別器對抗 | 生成影像、風格轉換、資料擴增 | 生成、對抗、轉換 |
| Autoencoder | 壓縮→重建 | 降維、異常偵測 | 重建、壓縮、異常 |
| VAE | 生成式自編碼 | 生成、潛在空間 | 生成、潛在 |
| Batch Norm | 各層輸入標準化 | 穩定加速訓練 | 正規化、穩定 |

---

## 9.7 考古題實戰

**1.** ChatGPT、Gemini 所依據的基礎模型技術？
(A) CNN  (B) RNN  (C) **Transformer/LLM** ✓  (D) GAN
→ 選 C。

**2.** 小明把照片轉成公仔風格影像，最可能用到？
(A) 線性迴歸  (B) **GAN** ✓  (C) KNN  (D) 決策樹
→ 選 B。風格轉換=GAN。

**3.** VAE 的運作流程？
(A) 無編碼  (B) **Encoder 壓縮至潛在分布→Decoder 生成** ✓  (C) 直接生成  (D) 不涉及重建
→ 選 B。

**4.** 加入 Batch Normalization 的主要作用？
(A) 增加非線性  (B) **將輸入正規化、使各層尺度一致、穩定加速訓練** ✓  (C) 加速推理  (D) 減少參數
→ 選 B。

**5.** Transformer 對「非常長輸入」推理時的考量？
(A) 完全不受長度影響  (B) **注意力運算成本高、可能較慢** ✓  (C) 只能依序處理  (D) 無法處理長文
→ 選 B。

---

## 9.8 活動與練習（2 小時配置建議）

- **00:00–00:25** 講授 9.1（Transformer）
- **00:25–00:45** 講授 9.2–9.3（GAN、Autoencoder/VAE）
- **00:45–01:00** 講授 9.4–9.5（Batch Norm、GRPO）
- **01:00–01:25** 實作/案例：分辨「生成/重建/語言/異常」題；討論 LLM 背後的 Transformer
- **01:25–01:45** 考古題測驗 10 題
- **01:45–01:55** 檢討
- **01:55–02:00** 預告第 10 單元（評估指標）＋收尾

---

## 9.9 本週重點速記

- **Transformer**＝注意力、可平行、長距離；是 **LLM 基礎**；長文推理成本高。
- **GAN**＝生成器+判別器對抗；生成/風格轉換/資料擴增。
- **Autoencoder**＝壓縮重建、降維、**異常偵測**；**VAE**＝生成式變體。
- **Batch Normalization**＝各層輸入正規化、穩定加速訓練。
- **GRPO**＝強化式優化 LLM 推理。
- 口訣：**語言→Transformer；生成→GAN；重建異常→Autoencoder。**

> 下一單元預告：第 10 單元「模型評估指標與過擬合」——Precision/Recall/F1/Accuracy 計算與判讀。
