# 第 13 單元｜大型語言模型 LLM①：RAG 與 Fine-tuning

> 單元目標：做對「RAG vs Fine-tuning」及 LLM 基礎的高頻考題。
> 對應考點：RAG（檢索增強生成）、Fine-tuning（微調）、LLM 基礎、生成式 AI 應用。
> 時數：2 小時

---

## 13.1 大型語言模型（LLM）是什麼

- **LLM（Large Language Model, 大型語言模型）**：以海量文字訓練的大規模神經網路，能「理解」並「生成」人類語言。
- **基礎架構**：**Transformer**（注意力機制，可平行處理、捕捉長距離文字相依）——詳見第 9 單元。
- **代表**：ChatGPT（GPT）、Gemini、Llama。
- **能力**：問答、摘要、翻譯、寫作、程式、推理。
- **特性**：參數量龐大、需大量算力與資料。

**考古題例**：
- 「ChatGPT、Gemini 對話型生成式 AI 的基礎模型技術？」→ **Transformer/LLM**。

---

## 13.2 預訓練與能力來源

- **預訓練（Pre-training）**：在海量通用文字上訓練，學到語言能力（通用知識）。
- 但預訓練模型的知識**有時效性**（訓練截止後的新資訊不知道）。
- 這正是引入 RAG / Fine-tuning 的背景。

---

## 13.3 RAG（Retrieval-Augmented Generation，檢索增強生成）★核心

**原理**：查詢時，先從外部知識庫（如向量資料庫）**檢索（Retrieve）**相關資料，再一起交給 LLM **生成（Generate）**答案。

```
使用者問題 → 檢索知識庫(向量DB) → 相關文件 → LLM 生成答案
```

**特色/優勢**：
- **不需重新訓練模型**，只要更新外部知識庫。
- 能**即時反映最新資料**（新法規、新文件）。
- 可引用來源、降低幻覺。
- 適合「知識問答、法遵查詢、客服」等需要最新/特定內容的情境。

**考古題例**（超高頻）：
- 「企業建置 RAG 的法遵知識問答系統，整合最新法規」→ **RAG**。
- 需「**即時反映最新法規/文件內容、不需重訓**」→ **RAG**。

> 口訣：**需要最新內容、即時更新、免重訓 → RAG。**

---

## 13.4 Fine-tuning（微調）★核心

**原理**：在預訓練模型基礎上，用**特定領域資料「繼續訓練」**，調整模型**權重**，讓模型學到特定風格/格式/領域語言。

**特色/限制**：
- **需要重新訓練**（即使規模小）。
- 課程知識有**時效性**——訓練完後的「新資料」不自動反映，需再微調。
- 適合「讓模型具備特定語氣、格式、領域術語」。

**考古題例**：
- 「讓 LLM 以特定格式/語氣輸出」→ **Fine-tuning**。
- 「公司法規每月更新，Fine-tuning 無法立即反映新內容」→ 為 Fine-tuning 的**限制**。

---

## 13.5 RAG vs Fine-tuning 對照 ★核心

| 面向 | RAG | Fine-tuning |
|------|-----|-------------|
| 是否改模型權重 | ❌ 不改，外接知識庫 | ✅ 改權重、重新訓練 |
| 更新最新資料 | **即時**（換知識庫即可） | 慢（需重訓） |
| 學特定風格/格式 | 較弱 | **適合** |
| 成本 | 需管理知識庫 | 需算力重訓 |
| 適用 | 知識問答、最新法規、客服 | 特定語氣/格式/領域術語 |

**選擇判讀**：
- 「**最新內容、即時反映、免重訓**」→ **RAG**。
- 「**特定風格/格式/領域語言**」→ **Fine-tuning**。

---

## 13.6 生成式 AI 應用情境（判讀）

| 應用 | 是否生成式 | 常配技術 |
|------|-----------|----------|
| 自動產出會議摘要 | ✅ 生成式 | RAG/LLM |
| 產品概念圖/服裝設計 | ✅ 生成式 | 生成模型/GAN |
| 客服助理回覆 | ✅ 生成式 | RAG/LLM |
| 新聞摘要與評論 | ✅ 生成式 | LLM |
| 輿情分析（判讀貼文情緒） | 分析型為主 | NLP |

**考古題例**：
- 「某設計公司導入生成式 AI 自動產出產品概念圖與風格圖」→ 生成式影像（LLM/擴散/GAN）。
- 「企業導入生成式 AI 自動產出會議摘要並規劃建置 RAG」→ 生成式＋RAG。

---

## 13.7 考古題實戰

**1.** 企業需「法規每月更新」的知識問答系統，最適合？
(A) Fine-tuning  (B) **RAG** ✓  (C) 兩者皆不適用  (D) CNN
→ 選 B。RAG 即時反映最新內容。

**2.** 想讓 LLM 以特定「格式/語氣」輸出？
(A) RAG  (B) **Fine-tuning** ✓  (C) 貝氏  (D) 分群
→ 選 B。

**3.** RAG 的主要特色？
(A) 重新訓練模型  (B) **從外部知識庫檢索後再生成、不重訓即可更新** ✓  (C) 只能影像  (D) 無需資料
→ 選 B。

**4.** ChatGPT、Gemini 所依據的基礎技術？
(A) CNN  (B) 決策樹  (C) **Transformer/LLM** ✓  (D) GAN
→ 選 C。

**5.** Fine-tuning 的缺點？
(A) 無法生成長文  (B) **訓練後的「新資料」不自動反映、須重訓** ✓  (C) 不需算力  (D) 無法調整
→ 選 B。

---

## 13.8 活動與練習（2 小時配置建議）

- **00:00–00:20** 講授 13.1–13.2（LLM、預訓練與時效限制）
- **00:20–00:45** 講授 13.3（RAG 原理與優勢）
- **00:45–01:05** 講授 13.4–13.5（Fine-tuning、對照表）
- **01:05–01:25** 講授 13.6（生成式應用判讀）＋案例討論
- **01:25–01:50** 考古題測驗 10 題
- **01:50–02:00** 檢討、預告第 14 單元（LLM②）＋收尾

---

## 13.9 本單元重點速記

- **LLM**＝大規模語言模型，基礎為 **Transformer**。
- **RAG**＝外接知識庫檢索→生成；**即時更新、免重訓**。
- **Fine-tuning**＝調整權重、重新訓練；學**特定風格/格式**；新資料不自動反映。
- 選擇：**最新內容→RAG；特定風格→Fine-tuning**。
- 生成式 AI＝自動產出內容（摘要、設計、客服、新聞）。

> 下一單元預告：第 14 單元「大型語言模型 LLM②」——批次/即時推論、Flash Attention、MoE、GRPO、CLIP/DINO 等新技術。
