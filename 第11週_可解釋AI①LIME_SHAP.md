# 第 11 週｜可解釋 AI（XAI）①：LIME 與 SHAP

> 單元目標：建立可解釋 AI 概念，熟記 LIME 與 SHAP 的差異與適用情境。
> 對應考點：可解釋 AI★高頻、LIME、SHAP、替代模型（Surrogate）。
> 時數：2 小時

---

## 11.1 為什麼需要可解釋 AI（XAI）

- 深度學習/黑箱模型常「能預測但不說為什麼」。
- 在高風險領域（醫療診斷、信貸、法遵、資安）需要知道「為什麼這樣判」：
  - 醫療：醫師要能覆核 AI 判讀依據。
  - 信貸：客戶被拒時要知道原因。
  - 法遵：監管要求可解釋。
- **XAI（Explainable AI）**＝讓黑箱模型「可解釋」的技術。

**考古題**：
- 「醫療診斷決策支援系統等**高風險領域**」→ 需要高度**可解釋性**。

---

## 11.2 LIME（局部可解釋、與模型無關）★高頻

**全名**：Local Interpretable Model-agnostic Explanations

三個關鍵字拆解：
- **Local（局部）**：解釋**某一筆特定預測**，不是整個模型。
- **Interpretable（可解釋）**：產出人類能懂的解釋。
- **Model-agnostic（與模型無關）**：不管什麼模型（SVM、神經網路、隨機森林）都能套用。

**作法**：
1. 對某一筆要解釋的資料，在其附近擾動產生許多變體。
2. 用一個**簡易、可解釋的替代模型（Surrogate model，如線性模型）**去近似原黑箱模型在該局部區域的行為。
3. 用替代模型說明「該筆預測主要受哪些特徵影響」。

**考點**：
- LIME＝**局部解釋**＋**與模型無關（Model-agnostic）**。
- LIME 使用**替代模型（Surrogate）**解釋單筆預測。

**考古題例**：
- 「LIME 中 Surrogate model（替代模型）的意義？」→ 用**可解釋的簡易模型近似原模型在該點附近的行為**。
- 「LIME 最可能被用在何種情境？」→ 說明某一筆被拒絕的申請為何被拒。

---

## 11.3 SHAP（Shapley 加性解釋）★高頻

**全名**：SHapley Additive exPlanations

- 源自**合作賽局論的 Shapley 值**。
- **作用**：計算「**每一特徵對某一筆特定預測結果的貢獻度**」。
- 給出每個特徵的「貢獻分數」（可為正/負），說明該筆結果主要由哪些特徵拉高/拉低。
- 常用於金融（信用評分）、醫療解釋。

**考點**：
- SHAP＝**各特徵對「單一預測」的貢獻量**。
- SHAP 值可量化「每個特徵貢獻多少」。

**考古題例**：
- 「某信用評分用 SHAP 分析，想了解『對某一申請人，各輸入特徵對最終分數貢獻多少』」→ **SHAP**。
- 「SHAP 的 Shapley 值提供何種資訊？」→ 各特徵對該預測的**邊際貢獻度**。

---

## 11.4 LIME vs SHAP 對比表 ★核心

| 面向 | LIME | SHAP |
|------|------|------|
| 全名 | Local Interpretable Model-agnostic Explanations | SHapley Additive exPlanations |
| 範疇 | **局部**（單筆） | **局部**（單筆） |
| 核心概念 | 用**替代模型（Surrogate）**近似局部 | **Shapley 值**計算特徵貢獻 |
| 是否與模型無關 | 是（Model-agnostic） | 是 |
| 輸出 | 說明該筆主要受哪些特徵影響 | 每個特徵的**貢獻分數** |
| 理論基礎 | 局部線性近似 | 合作賽局論 Shapley 值 |

> ⚠️ 易混重點：
> - LIME 強調「用簡單替代模型近似局部」。
> - SHAP 強調「給出各特徵的貢獻分數（Shapley 值）」。
> 兩者都是「解釋單筆預測」。

---

## 11.5 高風險領域與 XAI（情境判讀）

- **醫療診斷**：需要 XAI 讓醫師理解、覆核。
- **金融信貸**：被拒客戶需要可解釋原因（也常配反事實，見第 12 週）。
- **法遵/合規**：需證明 AI 決策合理、可稽核。

**考古題**：
- 「下列何種情境最需要很高程度的可解釋性？」→ 醫療診斷、信貸風控等**高風險**情境。

---

## 11.6 考古題實戰

**1.** LIME 中「替代模型（Surrogate model）」的意義？
(A) 完整的所有特徵模型  (B) **用可解釋簡易模型近似原模型在該點附近的行為** ✓  (C) 與模型無關的生成器  (D) 全參數模型
→ 選 B。

**2.** 想了解「對某一申請人，各輸入特徵對最終分數貢獻多少」？
(A) LIME  (B) **SHAP** ✓  (C) PDP  (D) Saliency
→ 選 B。SHAP 給貢獻分數。

**3.** LIME 的特性是？
(A) 全局解釋、模型相關  (B) **局部解釋、與模型無關（Model-agnostic）** ✓  (C) 需要影像  (D) 只能解釋分類
→ 選 B。

**4.** 醫療診斷高風險系統，最需要？
(A) 快速推論  (B) **可解釋性（XAI）** ✓  (C) 大量參數  (D) 無監督
→ 選 B。

**5.** 下列何者是「與模型無關（Model-agnostic）」的局部解釋方法？
(A) SHAP  (B) **LIME** ✓  (C) PDP  (D) Saliency Map
→ 選 B（詳見第 12 週）。

---

## 11.7 活動與練習（2 小時配置建議）

- **00:00–00:20** 講授 11.1（為何需要 XAI、高風險領域）
- **00:20–00:45** 講授 11.2（LIME：局部、無關模型、替代模型）
- **00:45–01:05** 講授 11.3（SHAP：Shapley 值、特徵貢獻）
- **01:05–01:25** 講授 11.4（LIME vs SHAP 對比）＋案例討論
- **01:25–01:50** 考古題測驗 10 題
- **01:50–02:00** 檢討、預告第 12 週（PDP/Saliency/反事實）＋收尾

---

## 11.8 本週重點速記

- **XAI**＝讓黑箱模型可解釋，高風險領域（醫療/信貸/法遵）尤需。
- **LIME**＝**局部**＋**與模型無關（Model-agnostic）**＋用**替代模型（Surrogate）**。
- **SHAP**＝**Shapley 值**，各特徵對**單筆預測的貢獻分數**。
- 兩者皆「解釋單筆預測」；LIME 重「替代模型」，SHAP 重「貢獻分數」。

> 下一週預告：第 12 週「可解釋 AI②」——PDP、Saliency Map、反事實解釋，及四工具的綜合分辨。
